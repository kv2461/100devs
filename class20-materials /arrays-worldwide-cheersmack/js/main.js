//Create a function that takes in a number. The function should return an array that contains 
//every number from 1 to that number as seperate elements

function arrayfill(num){
    let array = [];
    for (let i=i;i<=num;i++){
        array.push(i);
    }
}